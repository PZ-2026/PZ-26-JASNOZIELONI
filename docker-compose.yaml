version: '3'

services:
  postgres:
    image: postgres:16.1
    container_name: postgres_db
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=ala
      - POSTGRES_DB=projekt_pz
    volumes:
      - ./backend/database:/docker-entrypoint-initdb.d
    ports:
      - '5434:5432'

